Submit GRC:
<%= form_for :grcr, url: {action: "addGrcr"}, html: {class: "dummy"} do |f| %>
	<%= f.label :clientPm, "Client PM" %>
	<%= f.text_field :clientPm %>
	
	<input type="hidden" name="woId" value="<%= wo['_id'] %>" />
	
	<%= radio_button_tag 'grcr[acceptance_status]', 'accepted' %> Accepted<br> 
	<%= radio_button_tag('grcr[acceptance_status]', "rejected") %> Rejected <br>
	
	<%#= f.text_area :body, size: "60x12" %>
	<%= f.submit "Submit" %>
<% end %>